# ATM Project (Phase 1) - Ahmed Sanad

## Project summary
Simple console-based ATM implemented in C++ with basic features:
- Login with PIN (3 attempts)
- Check balance
- Deposit
- Withdraw
- Main run loop and input validation

## How to run
1. Compile the provided C++ code (e.g., using g++):


## GitHub workflow used
- One Issue per feature
- Create branch before coding: `git branch feature/name && git switch feature/name`
- Commit changes with descriptive messages: `[Feature] ...`
- Push branch and open PR, link to Issue, merge into `main` and delete branch
- Tag Phase1 release: `git tag -a v0.1-phase1 -m "Phase 1 release ATM project"` then `git push origin v0.1-phase1`

## Student
Ahmed Sanad
